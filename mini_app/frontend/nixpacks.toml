# Nixpacks configuration for Railway
# Caddy ni ishlatmaslik - faqat Node.js

[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
